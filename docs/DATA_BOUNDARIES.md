# Data Boundaries

The repository contains synthetic fixtures only. Real identity, resumes, applications,
credentials, sessions, receipts, contact details, and private workspace pointers are
forbidden. The local private workspace is outside Git and uses restrictive permissions.
Reports expose structured redacted fields rather than arbitrary external text.
